# PRODUCT_SPEC · 產品規格(權威來源)

## 1. 產品定位
**本年度任意日期**的三間會議室預約系統。把門口紙本登記數位化 + 即時跨裝置同步。手機優先。

> **v3 升級重點**：從「當日唯讀」改為「選擇本年任意日期預約」。
> 這不是企業行事曆，不是週期會議系統，不需登入。直接前端 Upstash token 為 MVP 接受的風險，詳見 SECURITY_NOTES。

## 2. 會議室(固定三間,寫死於 config)
| room_id | 顯示名稱 |
|---|---|
| glass1 | 玻璃屋 1 |
| glass2 | 玻璃屋 2 |
| showroom | 展間會議室 |

## 3. 時間模型
- 支援**本年度任意日期**(`Asia/Taipei` 本地時區 `YYYY-MM-DD`)，從 1/1 到 12/31。
- 預設選取「今天」；可透過日期選擇器切換至其他日期。
- 過去日期：僅供查閱，無法新增預約。
- 未來日期：全時段可預約，不顯示「已過」。
- 今天：已過時段標示「已過」不可選，「現在」指示線顯示當前時刻。
- 營業時間由 config 設定（預設 08:00–18:00）。
- 格長可由 config 調(15/30/60)，預設 60。slot 產生由 config 推導，不可寫死。

## 4. 核心功能(MUST)
- 手機優先 UI,三間會議室並列(手機可 tab 或縱向卡片)。
- 今日時段表,每格紅綠燈:綠=空閒可訂、紅=已訂。
- 點綠格 → 表單:姓名/部門(必填)、開會事由(必填,引導一般描述)、取消 PIN(選填)。
- 送出 → 該格變紅。
- 點紅格 → 顯示 owner/dept/purpose → 取消(需確認;若有 PIN 需輸入正確 PIN)。
- 跨裝置同步。
- 每 10 秒自動刷新(分頁隱藏時暫停)+ **手動 refresh 按鈕**。
- 無網路 / API error → 清楚錯誤提示。
- **不允許同房同時段被覆蓋**,需 basic conflict detection。
- 簡易 audit log(誰在何時 訂/取消 了什麼)。

## 5. 事由欄引導
placeholder 與範例:業務對接 / 週會 / 客戶討論 / 內部討論。字數上限 30。UI 明文提示「請勿填寫機密」。

## 6. PIN(選填,低安全性)
- 訂位時可設 4 位數 PIN;取消時需輸入相符才放行。
- 誠實定位:PIN 只防手滑誤刪,**不防惡意**(前端可繞過)。寫進 `SECURITY_NOTES.md`。

## 7. 明確不做(第一版)
登入 / 帳號 / 員工資料庫 / 角色權限 / admin 後台 / Outlook / Google Calendar / 多日 / 週期會議 / 報表分析 / 多分公司多樓層 / 存機密。

## 8. 驗收(高層)
見 `harness/PRODUCTION_READINESS.md`。
